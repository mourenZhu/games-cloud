<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhumouren.games.cloud.moment.mapper.LikesMapper">

    <select id="getMomentLikeNumsMap" resultType="HashMap">
        select moment_id, count(*) as likeNums
        from moment_likes
        group by moment_id
    </select>

    <select id="getMomentLikeUsernamePage" resultType="String">
        select username
        from moment_likes
        where moment_id = #{momentId}
    </select>

    <select id="getUserLikeMomentIdPage" resultType="Long">
        select moment_id
        from moment_likes
        where username = #{username}
    </select>

</mapper>
